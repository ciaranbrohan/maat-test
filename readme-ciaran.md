backend dependencies

-

frontend dependencies

-

Steps to build

1. Database 
 -  check that docker-compose password matches .env password
 - ```cd backend && docker compose up -d```
 - Expected: `postgres` container starts, `docker compose ps` shows it as `running`
